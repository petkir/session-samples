# Azure SDK packages (Python 3.9 compatible)
azure-search-documents==11.4.0
azure-identity==1.15.0
azure-core==1.29.0

# HTTP client for direct API calls
aiohttp==3.9.0

# PDF processing
PyPDF2==3.0.1
pdfplumber==0.9.0

# Image processing
Pillow==10.0.0
pdf2image==1.16.0
pymupdf==1.23.0

# Text processing and chunking (Python 3.9 compatible)
langchain==0.1.20
langchain-text-splitters==0.0.1
tiktoken==0.5.0

# Environment and configuration (Python 3.9 compatible)
python-dotenv==1.0.0
pydantic==2.0.3
pydantic-settings==2.0.3

# Logging and monitoring
structlog==23.1.0
rich==13.0.0

# Async support
asyncio-throttle==1.0.2

# Data handling (Python 3.9 compatible)
pandas==1.5.3
numpy==1.24.4

# Note: base64 is a built-in Python module, no need to install
